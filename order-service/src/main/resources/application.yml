server:
  port: 8081
spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5433/order_service_db
    username: postgres
    password: postgres
  jackson:
    serialization:
      indent-output: true
  rabbitmq:
    host: localhost
    port: 5672
    username: rabbit
    password: rabbit
springdoc:
  swagger-ui:
    disable-swagger-default-url: true
  api-docs:
    enabled: true